def main(data:str):
    """
    The data is from the file. Return the numbers as a list type.
    Args:
        data: str
    Returns:
        list: return answer
    """
    k = []
    for i in f :
        if i.isdigit():
            k.append(i)
    return k
f = open('txt_file/data03.txt').read()

    
# Read data from file